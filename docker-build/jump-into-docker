#!/bin/sh

# assume in this directory
# just do it with no figuring things out.


# straight into oraclelinux:8

sudo docker run --rm -it --network=host --hostname=docker-builder -v $PWD:/workspaces/mysql-server oraclelinux:8 bash
